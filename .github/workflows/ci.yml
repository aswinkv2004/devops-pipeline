YAML
name: ci pipeline
 on:
  push:
   branches: [main]
jobs:
    build:
    run-on:ubunta-latest

    steps:
      
      -name:checkout code
      uses:actions/checkout@v5

      -name:setup node.js
        uses:actions/setup-node@v3\
        with:
          node-vesion:16

       -name:install dependencies
        run:npm install

      -name:run test
      run:npm test     